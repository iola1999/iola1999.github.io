# build output
dist/
_site/

# 构建时生成的资源（OG 卡片 / 图片 webp 变体），交由 prebuild + astro build 重新生成，
# 不入库以免每次加文章/图片产生二进制 diff。品牌资源(favicon/social-card)仍保留提交。
public/og/
public/optimized/

# dependencies
node_modules/

# astro
.astro/

# misc
.DS_Store
*.log
.env
.env.production
